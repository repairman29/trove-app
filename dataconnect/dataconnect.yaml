specVersion: "v1"
serviceId: "trove-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "trove-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
